-- =====================================================================
--  AZ Accounting — سبب رفض الإدخال (يكتبه المراجِع، يراه الموظف والمدير)
--  شغّل هذا الملف مرة واحدة في: Supabase Dashboard → SQL Editor (آمن للتكرار)
-- =====================================================================

alter table public.pending_entries add column if not exists reject_reason text;

-- =====================================================================
--  انتهى.
-- =====================================================================
