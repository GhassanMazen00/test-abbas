# ربط AZ Accounting بقاعدة بيانات Supabase

هذا الدليل يجهّز الخادم (Supabase) ليخزّن العملاء والفواتير والدفعات، مع تسجيل دخول حقيقي
باسم مستخدم وكلمة مرور وأدوار (موظف / مدير).

> بعد إتمام الخطوات، أرسل لي **Project URL** و **anon public key**، وسأربط الواجهة بها وأختبرها.

---

## 1) إنشاء المشروع
1. ادخل إلى <https://supabase.com> وسجّل حساباً (مجاني).
2. اضغط **New project**، اختر اسماً وكلمة مرور لقاعدة البيانات ومنطقة قريبة، ثم **Create**.
3. انتظر دقيقة حتى يجهز المشروع.

## 2) إنشاء الجداول والصلاحيات
1. من القائمة الجانبية افتح **SQL Editor** → **New query**.
2. انسخ محتوى الملف [`supabase/schema.sql`](supabase/schema.sql) بالكامل والصقه، ثم اضغط **Run**.
3. يجب أن تظهر رسالة نجاح (Success). هذا ينشئ الجداول + سياسات الحماية (RLS) + إنشاء
   ملف تعريف تلقائي لكل مستخدم جديد.

## 3) تفعيل الدخول بالبريد وإيقاف تأكيد البريد
بما أننا نستخدم أسماء مستخدمين (نحوّلها داخلياً إلى بريد `اسم_المستخدم@azaccounting.app`)،
عطّل تأكيد البريد حتى يعمل الدخول مباشرةً:
1. **Authentication → Sign In / Providers → Email**: تأكد أنه مُفعّل.
2. أوقف **Confirm email** (Email confirmation) ثم احفظ.

## 4) إنشاء مستخدمي الدخول
**Authentication → Users → Add user** (اختر **Auto Confirm User = ON**):

| المستخدم | البريد (email) | كلمة المرور |
|----------|----------------|--------------|
| موظف | `<اسم_الموظف>@azaccounting.app` | تختارها |
| مدير | `<اسم_المدير>@azaccounting.app`  | تختارها |

- الجزء قبل `@` هو **اسم المستخدم** الذي سيُكتب في شاشة الدخول.
- يجب أن يكون النطاق بعد `@` مطابقاً لقيمة `AUTH_EMAIL_DOMAIN` في `js/config.js`
  (الافتراضي `azaccounting.app`).

ثم اجعل حساب المدير مديراً — نفّذ في **SQL Editor** (استبدل الاسم):
```sql
update public.profiles set role = 'manager' where username = '<اسم_المدير>';
```

## 5) نسخ المفاتيح
افتح **Project Settings → API** وانسخ:
- **Project URL** (مثل `https://abcd1234.supabase.co`)
- **anon public** key (مفتاح طويل يبدأ بـ `eyJ...`)

## 6) وضعها في التطبيق
افتح `js/config.js` واملأ:
```js
window.APP_CONFIG = {
  SUPABASE_URL: "https://abcd1234.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGciOi...",
  AUTH_EMAIL_DOMAIN: "azaccounting.app"
};
```

---

### ملاحظات
- المفتاح **anon public** مخصّص للاستخدام في المتصفح وآمن للنشر — الحماية الحقيقية عبر سياسات RLS في قاعدة البيانات (موجودة في `schema.sql`).
- الصلاحيات: أي مستخدم مسجّل يضيف عملاء/فواتير/دفعات؛ أما **التعديل والحذف فللمدير فقط** (مطبَّقة على مستوى قاعدة البيانات).
- عند ترك `SUPABASE_URL`/`SUPABASE_ANON_KEY` فارغين، يبقى التطبيق يعمل بالوضع التجريبي المحلي.

**الخطوة التالية بعد إرسال المفاتيح:** أربط الواجهة بـ Supabase (مصادقة + قراءة/كتابة البيانات) وأختبر كل المسارات.
